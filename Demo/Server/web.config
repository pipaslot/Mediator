<configuration>
  <system.webServer>
    <security>
      <requestFiltering>
        <!-- Allow up to 200MB (209715200 bytes) -->
        <requestLimits maxAllowedContentLength="209715200"/>
      </requestFiltering>
    </security>
  </system.webServer>
</configuration>
